// Select color input
// Select size input

// When size is submitted by the user, call makeGrid()

function makeGrid() {

// Your code goes here!
    var val = document.getElementById("sizePicker");

        var h = val.elements[0].value;
        var w = val.elements[1].value;

            document.getElementById("pixelCanvas").innerHTML = h;
            document.getElementById("test").innerHTML = w;
        
  var colz = document.getElementById("sizePicker");
  
        var color = val.elements[0].value;

       while(h > 0){

            var row = "<tr>";

            while(w > 0){
	            var col = "<td> </td>";
            }
        }


}
